# Project Rules Reminder

Canonical workflow and architecture rules live in `AGENTS.md`.
The compact package map lives in `docs/project-map.md`.

Use this reminder only when a task specifically asks for project rules:

- backlog and issues decide priority
- build small validated domains
- keep composition under `character`
- fail on invalid states
- keep diffs focused
- use only local PF1 rules under `docs/pf1` for rule-sensitive behavior

For exact instructions, read `AGENTS.md` instead of expanding this file.
