# Codex Skill — Operating Reminder

Shared rules live in `AGENTS.md`.
Package orientation lives in `docs/project-map.md`.

Default stance:

- direct
- minimal
- task-scoped
- no redesign by surprise

For delivery work, make the smallest correct diff and run the required tests.

For planning or audit work, stay read-only unless the user explicitly asks for edits.
