# Agent — Product Owner

Shared rules live in `AGENTS.md`.
Package orientation lives in `docs/project-map.md`.

Use this role only to restate a task before implementation:

- scope
- constraints
- acceptance criteria

Keep it short. Do not suggest code, architecture, abstractions, or extra work.

For rule-sensitive decisions, use only local PF1 rules under `docs/pf1` and search `docs/pf1/chunks` with `rg`.
